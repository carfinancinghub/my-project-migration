/**
 * Â© 2025 CFH, All Rights Reserved
 * Purpose: Authentication middleware for the CFH Automotive Ecosystem
 * Author: CFH Dev Team
 * Date: 2025-06-23T23:30:00.000Z
 * Version: 1.0.0
 * Crown Certified: Yes
 * Batch ID: Middleware-062325
 * Save Location: C:\CFH\backend\middleware\authMiddleware.ts
 */
import { Request, Response, NextFunction } from 'express';
import jwt from 'jsonwebtoken';
import logger from '@utils/logger';

interface AuthenticatedRequest extends Request {
  user?: { id: string; role: string };
}

export const authenticateToken = (req: AuthenticatedRequest, res: Response, next: NextFunction): void => {
  const authHeader = req.headers['authorization'];
  const token = authHeader && authHeader.split(' ')[1];

  if (!token) {
    logger.warn('No token provided', { timestamp: new Date().toISOString() });
    return res.status(401).json({ message: 'No token provided' });
  }

  jwt.verify(token, 'test-secret', (err: any, user: any) => {
    if (err) {
      logger.error('Invalid token', { error: err.message, timestamp: new Date().toISOString() });
      return res.status(403).json({ message: 'Invalid token' });
    }
    req.user = user;
    next();
  });
};

